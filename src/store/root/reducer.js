import * as actions from "./actions";

export const initialState = {};

export const rootReducer = (state, action) => {
  switch (action.type) {
    case "":
      break;

    default:
      throw new Error();
  }
};
